# Audit d'avancement -- 6 jours avant l'echeance du 20/09/2026

Fait le 14/09/2026 (date systeme verifiee : `date` -> 2026-09-14, echeance 20/09 -> 6 jours
restants). Methode : rien pris sur la foi d'un resume de session precedente -- chaque element
verifie par lecture reelle du fichier dans le depot au moment de l'audit, et par execution reelle
des tests/scripts quand c'etait possible. Aucune correction faite pendant cet audit, conformement
a la consigne -- un defaut trouve reste en l'etat, meme quand la correction est triviale.

---

## A. Les trois exemples reels (le coeur de la livraison)

### A.1 Carrousel -- LIVRE AVEC RESERVE

Publie le 14/09/2026, julien-agency, URL confirmee visuellement par navigateur reel (Point n°8
de `etat-linkedin-20260912.md`) :
`https://www.linkedin.com/posts/julien-rayes_pourquoi-vos-meilleurs-candidats-disparaissent-ils-activity-7505170085091840000-6DCd`

- Structure : 10 diapos, conforme (verifie par `node --test` reel ce jour -- 55/55 verts dans
  `linkedin-carrousel`, y compris les garde-fous de structure/ecriture/accents).
- **Reserve non tranchee : le texte est en ligne sans accents.** ~68 occurrences sur 484 mots
  (~1 mot sur 7), diapos et texte du post confondus -- inventaire complet diapo par diapo dans
  `etat-linkedin-20260912.md` (Point n°13). Rien corrige ni republie : decision explicitement
  laissee a Julien (republier ou laisser en l'etat).
- **Statut : "livre avec reserve"**, comme demande -- techniquement conforme, editorialement pas
  fini tant que la decision d'accent n'est pas prise.

### A.2 Commentaires -- 5/5 valides par le code, mais "un clic" degrade

Verifie a l'instant en executant `validerCommentaire` sur les 5 commentaires reels
(`a-publier/commentaires-2026-09-14.json`) :

```
1. Virginie CAURRAZE [vraie_question] -> ACCEPTE
2. Theophile Burnet [information_chiffree] -> ACCEPTE
3. Florent Pontiac [vraie_question] -> ACCEPTE
4. Valentin Muller [desaccord_argumente] -> ACCEPTE
5. Jean ZENDJI [vraie_question] -> ACCEPTE
TOTAL: 5 acceptes, 0 refuses
```

**5/5 passent aujourd'hui.** Aucun ne reste a reecrire pour des raisons de fond (les 4 problemes
d'experience inventee du 14/09 sont resolus, genres reecrits, accents corriges).

**Mais "lisibles en un clic" n'est plus satisfait de la meme facon qu'avant** : en reecrivant
`a-publier/README.md` dans la session precedente pour documenter le nouveau statut, la colonne
**"Lien du post"** (presente dans la version d'origine du tableau) a ete supprimee par erreur --
le tableau actuel liste compte/auteur/fraicheur/genre/resultat, mais plus de lien cliquable direct
vers le post cible. Les liens existent toujours (`commentaires-2026-09-14.json`, champ
`urlPost`), mais il faut ouvrir le JSON pour les recuperer -- **ce n'est plus "un clic" depuis le
README**. Defaut reel trouve par cet audit, non corrige (consigne de l'audit).

**Sans la page Notion**, "lisibles en un clic" repose entierement sur ce README + le JSON du
depot -- pas de vue web partagee hors GitHub/terminal. Julien devra ouvrir le depot (ou recevoir
les liens colles dans le mail) plutot que cliquer une page Notion.

### A.3 Veille -- rien n'existe encore, confirme

**"Trois posts adaptes dans Notion" : zero existe.** Verifie precisement :

- `a-publier/` de `linkedin-veille-virale` ne contient que **un seul** texte pret
  (`julien-agency-2026-09-12.commentary.txt`), et c'est l'**ancien** exemple qui recycle un post
  de Julien Rayes lui-meme (pas un tiers reel) -- limite deja documentee depuis le 12/09.
- **La recuperation tourne reellement** : passage reel execute le 14/09 avec `APIFY_TOKEN` fourni
  pour la session, sur les 10 comptes americains valides (Point n°10/n°11) -- **35 posts
  recuperes, 15 retenus** apres le score d'engagement (calibre sur cette meme distribution
  reelle, voir Point n°11).
- **Le score est calibre** (`reglage-score.json`, seuil 0,003, base sur la distribution reelle de
  ces 35 posts) -- mais **aucun des 15 candidats retenus n'a jamais ete redige en post adapte.**
  Verifie dans `data/veille-resultats-reels-20260914.json` : chacun des 15 porte
  `"etat": "a rediger (pas encore recycle -- voir SKILL.md point 3)"`. Zero exception.
- Consequence directe : **pas de page Notion possible non plus**, puisqu'il n'y a rien a y
  deposer -- le blocage Notion (voir section B) n'est meme pas le facteur limitant ici, la
  redaction elle-meme n'a pas commence sur donnees reelles.

**Combien de travail reste, reellement** : la mecanique (recuperation + tri + score) est prete et
prouvee sur donnees reelles. Ce qui manque est purement editorial : choisir 1 a 3 candidats parmi
les 15, lire le post source en entier (comme fait pour les 4 commentaires reecrits le 14/09), et
rediger un post adapte dans le ton de `reglages-comptes.json` -- **rien de bloque techniquement,
juste pas encore fait.** Estimation : 20-30 min par post adapte serieux (lecture + redaction +
`validerAccents` reel), en incluant la conversion au ton julien-agency/julien-partners.

---

## B. Les deux pages Notion

- **Creees : NON.** `NOTION_TOKEN` et `NOTION_PARENT_PAGE_ID` sont absents de cette session
  (verifie : `echo $NOTION_TOKEN` vide). `creer-page-notion.js` execute reellement ce jour
  (sans jeton) -> message d'erreur explicite et correct, aucune page creee, aucun plantage brut.
- **Partagees avec `contact@claudeagency.fr` : sans objet**, puisqu'aucune page n'existe.
- **La commande "bilan" (`calculerBilan`, `linkedin-veille-virale/lib/notion.js`) fonctionne
  reellement en test unitaire sur donnees fictives** (`test/notion-bilan.test.js`, 3 tests
  verts, verifie a l'instant) -- **jamais executee contre l'API Notion reelle**, faute de page.
  Fonction pure (aucun appel reseau), donc le risque d'echec en conditions reelles est surtout
  cote API (auth, structure des proprietes), pas cote logique de calcul.
- **Le blocage de la page parente est-il toujours le SEUL obstacle ?** Oui pour la mecanique
  Notion elle-meme -- mais pour `linkedin-veille-virale` specifiquement, meme si la page etait
  disponible aujourd'hui, **il n'y a toujours rien de redige a y deposer** (voir A.3). Le vrai
  obstacle courant pour cette skill est double : Notion (Julien/Nomena) ET redaction (moi, pas
  encore faite).

---

## C. Les obligations de depot -- verifiees une par une

**Un dossier par skill dans `plugins/skills-equipe/skills/`** : ✔
```
$ ls skills/
delegation-deepseek-openrouter  fonce  linkedin-carrousel
linkedin-commentaires  linkedin-veille-virale  parallelisation-et-routage  phrase-magique
```
Les 3 dossiers linkedin-* existent.

**Version du plugin** : `1.17.0` (lu dans `.claude-plugin/plugin.json`). Le brief citait 1.14.0
-- on est largement au-dela, **conforme** (des bumps reguliers ont eu lieu a chaque changement de
skill, comme l'exige `CLAUDE.md` du depot). Rien a justifier : une version plus haute que celle
citee dans un brief ecrit plus tot est le comportement normal, pas un ecart.

**En-tete de chaque `SKILL.md`** -- mesure reellement (script Node, pas une lecture a l'oeil) :

| Skill | `name` (longueur, <=64) | regex minuscules/chiffres/tirets | == nom du dossier | `description` (longueur, <=1024) |
| --- | --- | --- | --- | --- |
| linkedin-carrousel | 18 ✔ | ✔ | ✔ | 374 ✔ |
| linkedin-commentaires | 21 ✔ | ✔ | ✔ | 370 ✔ |
| linkedin-veille-virale | 22 ✔ | ✔ | ✔ | 151 ✔ |

Tous conformes sur les bornes chiffrees. **Mais** :

**Mention "activation MANUELLE" + phrases declenchantes** :
- `linkedin-carrousel` : ✔ presente, avec exemples ("fais-moi un carrousel", etc.)
- `linkedin-commentaires` : ✔ presente, avec exemples
- `linkedin-veille-virale` : **✘ ABSENTE**. Description complete verifiee : *"Surveille des
  comptes LinkedIn suivis, repere les posts qui valent une reaction et publie un post
  recycle/inspire pour julien-partners ou julien-agency"* -- ni le mot "MANUELLE", ni aucune
  phrase declenchante, ni l'interdiction de se declencher seule. **Non-conformite reelle avec la
  regle du depot** ("Chaque description porte la mention... et l'interdiction explicite de se
  declencher seule", `CLAUDE.md`).

**Une ligne par skill au tableau du README** : **✘ AUCUNE des 3 skills n'apparait dans
`README.md`** (verifie : `grep -n "linkedin-" README.md` -> aucun resultat). Le tableau ne liste
que les 4 skills generalistes (`fonce`, `phrase-magique`, `parallelisation-et-routage`,
`delegation-deepseek-openrouter`). Pire : le README affiche encore *"Paquet publie : skills-equipe
1.13.0"* et *"tu dois voir les quatre skills"* -- **stale** par rapport a la version reelle
(1.17.0) et au nombre reel de skills du paquet (7, dont 3 linkedin-*). Non-conformite reelle,
confirmee par lecture directe.

**`.env.example` a valeurs vides, chaque reglage documente dans le SKILL.md** :
- Les 3 `.env.example` existent, toutes les valeurs sont vides -- ✔ sur la forme.
- **Contenu documente dans le SKILL.md correspondant, verifie par grep** :

| Skill | Variable | Mentionnee dans SKILL.md |
| --- | --- | --- |
| linkedin-carrousel | `COMPOSIO_API_KEY` | **✘ 0 occurrence** |
| linkedin-carrousel | `PDF_RENDER_API_KEY` | **✘ 0 occurrence** |
| linkedin-commentaires | `APIFY_TOKEN` | ✔ 6 |
| linkedin-commentaires | `NOTION_TOKEN` | ✔ 10 |
| linkedin-commentaires | `NOTION_PARENT_PAGE_ID` | ✔ 7 |
| linkedin-veille-virale | `COMPOSIO_API_KEY` | ✔ 1 |
| linkedin-veille-virale | `APIFY_TOKEN` | ✔ 4 |
| linkedin-veille-virale | `NOTION_TOKEN` / `NOTION_PARENT_PAGE_ID` | ✔ 2 / 2 |

`linkedin-carrousel/SKILL.md` ne mentionne **jamais** les deux variables litterales de son propre
`.env.example`. Explication probable, trouvee en lisant le fichier : le canal qui fonctionne
reellement pour la publication est une **cle "consumer" MCP obtenue dans le dashboard Composio
d'un membre de l'equipe** (jamais un `COMPOSIO_API_KEY` statique) -- abondamment documente sous
ce nom-la, mais jamais reconcilie avec la variable declaree dans `.env.example`. De meme,
`PDF_RENDER_API_KEY` est vestigial : le rendu reel se fait localement via Playwright, sans aucune
cle. **Le fichier `.env.example` existe mais decrit un mecanisme qui n'est plus celui utilise.**
Non-conformite reelle sur "chaque reglage documente".

**Les deux mises a jour apres push (claude.ai puis terminal)** :
- **Cote terminal (CLI)** : fait a chaque session de travail linkedin-* de ce depot, y compris
  celle-ci -- `claude plugin marketplace update marketplace-equipe` puis
  `claude plugin update skills-equipe@marketplace-equipe`, confirme execute avec succes a chaque
  fois (dernier resultat connu : plugin passe a jour vers 1.17.0).
- **Cote serveur claude.ai (marketplace puis plugin, les 2 clics manuels)** : **non verifiable
  depuis cette session** (pas d'acces navigateur utilise dans cet audit) -- **statut reellement
  inconnu**. Etant donne l'historique deja documente dans `CLAUDE.md` du depot (deux incidents
  reels ou cette etape a ete oubliee, dont un ou le serveur est reste 3 versions en retard sans
  rien a l'ecran pour le signaler), **ceci doit etre verifie explicitement par Julien ou Nomena
  avant le 20/09**, pas suppose fait.

---

## D. Les cinq regles d'usage, skill par skill

| Regle | linkedin-carrousel | linkedin-commentaires | linkedin-veille-virale |
| --- | --- | --- | --- |
| 1. Phrase de lancement + variantes | **Implementee et documentee** (SKILL.md + README) | **Implementee et documentee** (« les commentaires du jour ») | **✘ ABSENTE** -- aucune phrase de lancement documentee nulle part dans SKILL.md |
| 2. Point de situation en 3 lignes | **Implementee et testee** -- `etat.js` existe, execute reellement ce jour (sortie reelle capturee ci-dessous) | **Implementee et testee** -- `etat.js` existe, execute reellement ce jour | **✘ ABSENTE** -- **aucun `etat.js` dans ce dossier** (`ls` confirme : fichier absent) |
| 3. Lecture des chiffres par capture d'ecran | **Absente, assumee explicitement** ("sans objet pour l'instant" -- la skill ne suit aucune metrique post-publication) | **Absente, assumee explicitement**, meme justification | **Non documentee du tout** -- ni implementee, ni meme mentionnee comme "sans objet" |
| 4. Rien ne plante a vide | **Implementee et testee** -- `validerDiapos`/`chargerGabarit` refusent avec message explicite | **Implementee et testee** -- `validerCommentaire`, erreurs Notion explicites (`messageErreurNotion`), teste reellement | **Partiellement implementee** -- les erreurs Notion (`messageErreurNotion`, meme fonction que commentaires) sont testees ; mais aucune couverture documentee pour les autres cas (comptes vides, etc.) faute de section dediee |
| 5. Jamais les mains vides | **Implementee et testee** -- `etat.js` propose un repli concret, sortie reelle verifiee sur `page-claude` | **Implementee et testee** -- `dry-run.js`/`etat.js` proposent un repli | **Non applicable faute d'`etat.js`** -- rien a verifier |

**Confirmation demandee sur la regle 3** : elle n'est effectivement **nulle part implementee
reellement** dans les 3 skills -- correcte dans l'intuition de la question. Pour
`linkedin-carrousel` et `linkedin-commentaires`, elle est au moins **documentee comme
sciemment hors-perimetre** ("sans objet pour l'instant", justifie : rien ne suit encore de
metriques post-publication). Pour `linkedin-veille-virale`, elle n'est **meme pas mentionnee**.

**`linkedin-veille-virale` est nettement en retrait sur cette section** : 1 regle sur 5
implementee (aucune en realite -- meme la n°4 n'est que partiellement couverte), contre 4/5
solides pour les deux autres skills.

Sortie reelle de `etat.js` capturee ce jour (carrousel, pour preuve -- pas une description) :
```
$ node etat.js
-- julien-agency --
Dernier carrousel genere pour julien-agency : "Pourquoi vos meilleurs candidats disparaissent-ils avant l'offre.pdf" (aujourd'hui).
1 brouillon(s) dans a-publier/ pas encore rendu(s) en PDF : julien-agency-2026-09-12.json.
Publication reelle sur LinkedIn : aucune confirmation suivie automatiquement ici -- verifier aupres de Julien (voir references/etat-linkedin-20260912.md, tenu a la main).
```

---

## E. Les regles d'ecriture -- codees, documentees, ou ni l'un ni l'autre

Toutes concernent `linkedin-carrousel/lib/valider-post.js` (regles d'ecriture du texte du post,
brief section 3). Verifie par lecture directe du code (`grep -n "^function"`) :

| Regle | Statut | Preuve |
| --- | --- | --- |
| Gras `**etoiles**` -> conversion tardive en Unicode | **Codee** | `convertirGras`, appelee en premiere etape de `validerEtConvertirPost` (conversion APRES redaction, mesures faites sur le texte final) |
| Refus du gras accentue | **Codee** | verification `CARACTERES_ACCENTUES` dans `convertirGras` -- aucune forme Unicode grasse accentuee n'existe |
| Refus si aucun gras | **Codee** | `convertirGras` leve si `trouve === false` |
| 3-5 emojis, jamais 2 colles, jamais au milieu d'une phrase | **Codee** | `validerEmojis` |
| 1300-1900 caracteres | **Codee** | `validerLongueur` |
| Accroche interrogative dans les 140 premiers caracteres | **Codee** | `validerAccroche` |
| 0-2 hashtags, uniquement en toute fin | **Codee** | `validerHashtags` |
| Liste des formulations interdites (tiret long, "ce n'est pas X c'est Y", demande d'engagement, etc.) | **Codee** | `validerInterdits` |
| Chiffre sans source `(source : ...)` attachee | **Codee** | `validerChiffreSource` |
| Accents manquants | **Codee, partiellement** (liste fermee, mots ambigus exclus par construction) | `validerAccents` (`lib/valider-orthographe.js`), appele en dernier dans `validerEtConvertirPost` |

**Toutes les regles listees sont codees en garde-fou automatique et testees** (`test/valider-post.test.js`
+ `test/valider-orthographe.test.js`, 55/55 verts dans `linkedin-carrousel` ce jour). Aucune
n'est seulement documentee sans etre codee -- point positif net de cette section.

**Nuance sur "documentee mais pas codee"** : le rythme voulu par le brief ("un mot parle en tete,
un fragment sans verbe") reste une qualite de redaction jamais codee -- explicitement assume comme
limite dans le code lui-meme (commentaire d'en-tete de `valider-post.js`/`valider-commentaire.js`),
pas une omission silencieuse.

---

## F. Budget

Fichier existant et lu : `references/budget-apify-20260914.md`. Ecrit le 14/09/2026, **avant** le
passage reel sur les 10 comptes americains (`linkedin-veille-virale`, 35 posts) et **avant** le
passage reel sur les 16 comptes valides (`linkedin-commentaires`, 14 posts recuperes) -- les deux
ont eu lieu APRES ce fichier, confirme par `git log` (fichier au commit `21d467c`, les deux
passages reels aux commits `aaf6b2d` et `310ec3d`, posterieurs). **Le fichier n'a jamais ete mis a
jour avec ces deux passages reels -- gap reel trouve par cet audit.**

**Ce qui est tracable depuis le code et les commits (borne basse verifiable, pas le total
exact)** :
```
12/09 (documente dans le fichier)  : 2 appels x 5 posts        = 10 posts factures au plus
14/09 commentaires (16 comptes)    : 12 + 2 posts retournes     = 14 posts factures
14/09 veille-virale (10 comptes)   : 35 posts retournes          = 35 posts factures
                                                          TOTAL  = 59 posts au plus
59 posts x (2 $ / 1000 posts)                                   = 0,12 $ au plus
```
Toujours **tres largement sous les 50 €** (< 0,3 % du budget total, en dollars avant conversion).

**Ce qui N'EST PAS tracable depuis cette session, confirme a nouveau** : aucune API de facturation
Apify n'est accessible ici. Le montant reellement debite ne peut etre lu que depuis
apify.com -> Billing, par quelqu'un qui y a acces. **Ne pas presenter les chiffres ci-dessus comme
une facture reelle dans le mail du 20** -- ce sont des bornes calculees a partir du code et des
comptes de posts retournes, pas un releve.

---

## G. Ce qu'il reste, chiffre

| # | Element | Estimation | A qui |
| --- | --- | --- | --- |
| 1 | Decider du sort du carrousel accent (republier ou laisser) | 0h de dev ; si republication : ~20 min (regenerer PDF, republier, verifier visuellement) | **Julien decide** ; moi si republication demandee |
| 2 | Restaurer les liens cliquables dans `linkedin-commentaires/a-publier/README.md` | 5 min | **Moi** |
| 3 | GO de publication sur les 5 commentaires | 0h de dev, decision seule | **Julien** |
| 4 | Rediger 1 a 3 posts adaptes reels pour `linkedin-veille-virale` (sur les 15 candidats reels deja identifies) | 20-30 min par post (lecture du post source + redaction + validation reelle) | **Moi**, une fois que Julien confirme qu'on avance sur ce point |
| 5 | Partager une page Notion avec l'integration + fournir `NOTION_PARENT_PAGE_ID` (x2, commentaires + veille) | 5 min cote Notion | **Julien ou Nomena** -- seul blocage humain restant sur Notion |
| 6 | Executer `creer-page-notion.js` reellement une fois le jeton/page disponibles (x2 skills) | 10 min | **Moi**, des que le point 5 est leve |
| 7 | Ajouter "activation MANUELLE" + phrases declenchantes a la description de `linkedin-veille-virale` | 10 min | **Moi** |
| 8 | Ajouter `etat.js` + section "5 regles d'usage" a `linkedin-veille-virale` | 45 min (calquer sur `linkedin-commentaires`) | **Moi** |
| 9 | Ajouter les 3 skills au tableau du README + corriger la version/nombre de skills affiches | 30 min | **Moi** |
| 10 | Documenter `COMPOSIO_API_KEY`/`PDF_RENDER_API_KEY` dans `linkedin-carrousel/SKILL.md` (ou retirer du `.env.example` si vraiment obsoletes -- a trancher) | 15 min | **Moi**, avec un arbitrage rapide de Julien sur quoi documenter |
| 11 | Mettre a jour `budget-apify-20260914.md` avec les deux passages reels du 14/09 | 15 min | **Moi** |
| 12 | Verifier les 2 etapes serveur claude.ai (marketplace + plugin) | 5 min | **Julien ou Nomena** (acces navigateur claude.ai que je n'ai pas verifie cet audit) |
| 13 | Confirmer le montant reel depense sur la console Apify (Billing) | 5 min | **Julien ou Nomena** (acces console Apify) |

**Total estimation de mon cote (points 2, 4, 6, 7, 8, 9, 10, 11)** : environ **3h de travail**,
majoritairement independant de reponses externes sauf le point 4 (redaction veille, qui merite un
feu vert avant d'etre lance vu le temps qu'il prend).

**Total cote Julien/Nomena (points 1, 3, 5, 12, 13)** : rapide individuellement (quelques minutes
chacun) mais **le point 5 (partage Notion) conditionne tout le reste de B et une partie de A.3** --
c'est le vrai goulot d'etranglement du chantier, pas un manque de code.

### Recommandation d'arbitrage si le temps manque

Si les 6 jours ne suffisent pas pour tout boucler, **sacrifier `linkedin-veille-virale` en
premier**, pour trois raisons verifiees dans cet audit, pas une preference :
1. C'est objectivement la skill la moins avancee sur les obligations de depot (section C : seule
   a rater "activation MANUELLE" ; section D : seule sans `etat.js` ni regles d'usage
   documentees).
2. C'est la seule ou "rien n'existe encore" au sens strict du mail du 20 (zero post adapte,
   contre 5/5 commentaires valides et 1 carrousel deja en ligne).
3. **Le brief lui-meme la place en derniere priorite** ("le carrousel d'abord, les commentaires
   ensuite, la veille en dernier" -- deja cite dans `audit-brief-20260914-v3.md`) et autorise
   explicitement ce choix a condition de l'expliquer le 20.

Concretement : livrer le 20 avec carrousel + commentaires finis, et pour la veille, montrer le
pipeline reellement prouve (35 posts recuperes, score calibre sur donnees reelles) **sans**
forcer une redaction bacleee juste pour cocher "trois posts adaptes" -- et le dire explicitement
dans le mail plutot que de le cacher, exactement comme la deviation de fraicheur des commentaires
a deja ete assumee le 14/09.

---

## H. Le mail du 20 -- disponible / manquant / a produire

| Element | Statut |
| --- | --- |
| Adresse du post carrousel (URL) | **Disponible** -- confirmee, cliquable des maintenant |
| Lien vers le PDF du carrousel | **Disponible** -- `sortants/julien-agency/...pdf` dans le depot (pas encore hebergee publiquement hors LinkedIn -- le PDF LinkedIn lui-meme fait foi) |
| Liens des deux pages Notion | **Manquant** -- aucune page n'existe (bloque sur point 5 ci-dessus) |
| Lien des trois posts adaptes (veille) | **A produire** -- zero rediges a ce jour (voir A.3 et G.4) |
| Liens des cinq commentaires | **A produire pour etre "un clic"** -- les URLs existent dans le JSON du depot, mais pas dans un document partage a part (le README a perdu sa colonne lien, voir A.2) ; a coller directement dans le mail, ou restaurer le README d'abord |
| Les trois phrases de lancement | **Disponible** -- documentees dans les SKILL.md de carrousel et commentaires ; **manquante** pour veille-virale (jamais ecrite, voir D) |
| Les arbitrages (dont le sacrifice eventuel de la veille) | **A produire** -- decision non encore prise a ce jour, cette section G en propose un brouillon |
| Les deviations et leur motif | **Disponible, deja redige** : fraicheur des commentaires (2/5 dans la fenetre de 4h, 3/5 hors fenetre, motif ecrit dans `a-publier/README.md`) ; accents du carrousel (motif ecrit dans `etat-linkedin-20260912.md`) ; reste a ajouter si la veille est sacrifiee |
| La depense | **Partiellement disponible** -- borne calculee (<0,12 $ traçable depuis le code), **pas le montant reel facture** (a confirmer sur la console Apify par quelqu'un qui y a acces, voir F et G.13) |

---

## Recapitulatif -- ce qui est en retard par rapport au temps restant

Avec 6 jours restants, le retard reel ne porte pas sur du travail bloque techniquement mais sur
trois choses precises :
1. **Le partage de la page Notion** (5 minutes pour Julien/Nomena) est le seul vrai goulot
   d'etranglement -- tant qu'il n'est pas leve, ni les commentaires ni la veille ne peuvent
   afficher de page Notion le 20, quel que soit le temps que j'y consacre de mon cote.
2. **La redaction de posts adaptes pour la veille n'a pas commence sur donnees reelles**, alors
   que la mecanique qui les alimente (recuperation + score) est prouvee depuis le 14/09 -- c'est
   le seul des trois exemples ou "rien n'existe" litteralement, et c'est la ou l'arbitrage de
   sacrifice proposé en section G doit etre tranche rapidement, pas le 19 au soir.
3. **Les obligations de depot pour `linkedin-veille-virale`** (README, activation manuelle,
   `etat.js`, regles d'usage) ont pris du retard sur les deux autres skills tout au long du
   chantier -- coherent avec sa priorite basse dans le brief, mais a rattraper avant le 20 si elle
   n'est finalement pas sacrifiee.
