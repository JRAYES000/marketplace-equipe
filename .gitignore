# Les cles ne se commitent jamais : ce depot est public.
.env
.env.*
!.env.example

# Scratch local
sortants/

# Donnees reelles scrapees (posts LinkedIn de comptes reels via Apify) --
# vivent localement pour ne pas se reperdre entre sessions, jamais commit :
# contenu specifique a un compte reel, pas a publier dans un depot public.
**/linkedin-veille-virale/data/
**/linkedin-commentaires/data/

# Registre des echecs Composio/LinkedIn (17/09/2026, suite) -- meme raison que
# ci-dessus : donnee d'execution locale, jamais du code.
**/linkedin-carrousel/data/

# Dependances npm locales a une skill (package.json a cote, jamais commit)
node_modules/
